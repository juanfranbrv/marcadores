---
// src/pages/auth-success.astro
---
<html>
  <head>
    <meta charset="utf-8" />
    <title>Autenticación exitosa</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
  </head>
  <body class="bg-gray-50 flex items-center justify-center min-h-screen">
    <div class="bg-white p-8 rounded shadow text-center">
      <h1 class="text-2xl font-bold mb-4">¡Autenticación exitosa!</h1>
      <p class="mb-2">Redirigiendo a la aplicación...</p>
      <p class="text-xs text-gray-400">Si no eres redirigido automáticamente, <a href="/" class="text-blue-600 underline">haz clic aquí</a>.</p>
    </div>
    <script type="module">
      // Captura el token de la URL y lo guarda en localStorage
      const params = new URLSearchParams(window.location.search);
      const token = params.get('token');
      if (token) {
        localStorage.setItem('raindrop_access_token', token);
      }
      // Redirige a la home tras guardar el token
      setTimeout(() => {
        window.location.href = '/';
      }, 1200);
    </script>
  </body>
</html>
